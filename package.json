{
  "name": "skypath-hre",
  "version": "1.0.0",
  "private": true,
  "description": "SkyPath — Robert Gabriel Mugabe International Airport wayfinding kiosk",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/copy-classic.mjs",
    "preview": "vite preview",
    "verify": "node tests/skyverify.js",
    "typecheck": "node --check app.js"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
